# Backend API URL (Flask running locally on port 5000)
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000

# Production Backend API URL (Flask running on AMD's server)
# NEXT_PUBLIC_BACKEND_URL=https://dcgpu-lab.amd.com 